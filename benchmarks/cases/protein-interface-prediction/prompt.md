# Case: protein-protein interface prediction

## Research ask
Given two UNBOUND protein structures (or, if no structure, their sequences), predict which
**residue pairs are at the protein-protein interface** when they bind. Build me an algorithm.
Python, CPU only (no deep learning training).

## Constraints stated by the user
- Python, CPU only, no DL training.
- Input: two unbound structures or two sequences.
