# Case: spatial cell-type deconvolution (home-field, spatial)

## Research ask
I have spatial RNA-seq (spots are mixtures of cells) and a matched single-cell reference with
cell-type labels. I want to **estimate the cell-type proportions per spot**. Build me a
deconvolution algorithm. Python (scanpy/squidpy), CPU only, no DL.

## Constraints stated by the user
- Python (scanpy/squidpy), CPU only, no deep learning.
- Spatial RNA (spot-level, mixed) + a single-cell reference with labels.
